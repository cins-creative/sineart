/**
 * Fallback Suspense + `loading.tsx` — khớp layout phòng học (`.phc`), ink 6% + shimmer peach.
 */
export { PhongHocClassroomSkeleton as PhongHocSlugClassroomSectionSkeleton } from "./PhongHocClassroomSkeleton";
