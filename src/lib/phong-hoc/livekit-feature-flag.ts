/**
 * Tắt LiveKit tạm thời — phòng học native chưa sẵn sàng (cần server + app iOS GV).
 * Khi `true`: canvas hiện lớp phủ «Phòng học đang cập nhật» + luồng Google Meet.
 * Bật lại: đổi thành `false` sau khi LiveKit production đã cấu hình xong.
 */
export const PHONG_HOC_LIVEKIT_DISABLED = true;
